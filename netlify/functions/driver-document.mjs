import {driverStore,parseCookies,validToken,html} from './_shared.mjs';
export const config={path:'/driver-document/:id/:cat/:name'};
export default async(req,context)=>{const id=String(context.params?.id||''),c=parseCookies(req);if(!validToken(c.DRIVER_AUTH,'DRIVER:'+id))return new Response('Accès refusé',{status:403});const cat=String(context.params?.cat||''),name=decodeURIComponent(String(context.params?.name||''));const safe=name.replace(/[\\/]/g,'');const key=`docs/${id}/${cat}/${safe}`;const data=await driverStore().get(key,{type:'arrayBuffer'});if(!data)return html('<h1>Document introuvable</h1>',404);const meta=await driverStore().getMetadata(key).catch(()=>null);return new Response(data,{headers:{'Content-Type':meta?.metadata?.contentType||'application/pdf','Content-Disposition':`inline; filename="${safe.replace(/"/g,'')}"`}})};
