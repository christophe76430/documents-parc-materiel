import { MACHINES, TYPES, store, statusStore, extinguisherStore, hasExtinguisher, parseCookies, validToken, html } from './_shared.mjs';
export const config={path:'/export'};
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function statuses(){const out={};for(const id of Object.keys(MACHINES)){try{const r=await statusStore().get(`machine/${id}.json`,{type:'json',consistency:'strong'});out[id]=r?.enabled!==false}catch{out[id]=true}}return out}
async function exts(){const out={};for(const id of Object.keys(MACHINES)){if(!hasExtinguisher(id))continue;try{const r=await extinguisherStore().get(`machine/${id}.json`,{type:'json',consistency:'strong'});if(r?.expiry)out[id]=r.expiry}catch{}}return out}
export default async req=>{const c=parseCookies(req);if(!validToken(c.PARC_ADMIN,'ADMIN'))return html('<h1>Accès refusé</h1><p>Export réservé à l’administration.</p>',403);const active=await statuses(),ext=await exts();const {blobs}=await store().list({prefix:''});const rows=[];for(const b of blobs){const p=b.key.split('/');const m=MACHINES[p[0]];if(!m||active[m.id]===false)continue;const type=p[1]||'';const meta=(await store().getMetadata(b.key,{consistency:'strong'}).catch(()=>null))?.metadata||{};rows.push([m.name,m.group==='pelles'?'Matériel rail-route':m.group==='vehicules'?'Véhicule':'Camion',TYPES[type]?.label||type,meta.label||p.slice(2).join('/'),meta.expiry||''])}for(const [id,e] of Object.entries(ext)){if(active[id]===false)continue;const m=MACHINES[id];const [y,mo]=e.split('-');rows.push([m.name,m.group==='pelles'?'Matériel rail-route':'Camion','Extincteur','Extincteur',mo&&y?`${mo}/${y}`:''])}rows.sort((a,b)=>a[0].localeCompare(b[0],'fr')||a[2].localeCompare(b[2],'fr'));const body=rows.map(r=>`<tr>${r.map(x=>`<td>${esc(x)}</td>`).join('')}</tr>`).join('');const xls=`<!doctype html><html><head><meta charset="utf-8"></head><body><table border="1"><tr><th>Matériel</th><th>Type</th><th>Document</th><th>Fichier</th><th>Échéance</th></tr>${body}</table></body></html>`;return new Response(xls,{headers:{'Content-Type':'application/vnd.ms-excel; charset=utf-8','Content-Disposition':`attachment; filename="THN_parc_${new Date().toISOString().slice(0,10)}.xls"`}})};
